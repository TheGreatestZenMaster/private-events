<%= form_for(@current_user.invitations.build(attended_event_id: @event.id)) do |f| %>
    <%= f.hidden_field :attended_event_id, value: @event.id %>
    <%= f.submit "Attend", class: 'btn btn-primary' %>
<% end %>

 